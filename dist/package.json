{
  "name": "tencent-action",
  "version": "1.0.0",
  "description": "A simple GitHub Action without Rollup",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "compile": "mkdir -p dist && cp index.js dist/ && cp package.json dist/ && cd dist && npm install --omit=dev",
    "build": "npm run compile && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint ."
  },
  "keywords": ["github", "actions"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@reverse_game/tencent_cloud": "0.2.3"
  },
  "devDependencies": {
    "eslint": "9.33.0",
    "eslint-config-standard": "7.1.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
